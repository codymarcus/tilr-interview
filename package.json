{
  "name": "tilr-interview",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/codymarcus/tilr-interview.git",
  "license": "MIT",
  "scripts": {
    "setup-db": "cd server && node scripts/db.js && node_modules/.bin/sequelize db:migrate",
    "install-server": "cd server && yarn install",
    "install-client": "cd client && yarn install",
    "setup": "yarn install-server && yarn install-client && yarn setup-db",
    "start-server": "cd server && yarn start",
    "start-client": "cd client && yarn start"
  }
}
